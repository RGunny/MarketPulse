spring:
  application:
    name: alert-test
  data:
    mongodb:
      uri: mongodb://localhost:27017/alert_test_db
    redis:
      host: localhost
      port: 6379
      database: 2

# 테스트용 외부 의존성 비활성화
management:
  endpoints:
    enabled-by-default: false
    
logging:
  level:
    me.rgunny.marketpulse.alert: DEBUG
    org.springframework.data.mongodb: DEBUG